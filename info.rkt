#lang info

(define collection 'multi)

(define deps '("base"))
(define build-deps '())

(define version "0.0.0")

(define pkg-desc "Nemesis: a Racket-native, macOS-only web browser.")
(define pkg-authors '(pdelfino))

(define license 'BSD-3-Clause)
